'use strict';

/* eslint @typescript-eslint/no-var-requires: "off" */
const path = require('path');

module.exports = {
  apps: [
    {
      name: 'backend',
      script: 'npm run start'
    },
  ],
};
